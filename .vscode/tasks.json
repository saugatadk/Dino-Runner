{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "shell",
            "label": "Build and Run",
            "command": "${workspaceFolder}/build_and_run.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            },
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}